{
  "name": "invoicebackend",
  "version": "1.0.0",
  "description": "backend server for invoicer",
  "main": "index.js",
  
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon app.js",
    "start": "node backend/app.js", 
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "cd backend && npm install", 
    "deploy": "npm run build:frontend && npm run build:backend && render deploy --build-dir invoicerapp"
  },
  "author": "shahzad",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.1",
    "mongodb": "^5.7.0",
    "mongoose": "^7.4.2",
    "nodemailer": "^6.9.5",
    "nodemon": "^3.0.1"
  }
}
